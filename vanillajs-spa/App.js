function App($target) {
  const { pathname } = location;
  console.log(pathname);
  const init = () => {};
  const render = () => {
    console.log("@@@@@@");
  };

  render();
}

export default App;
